# previews/screens/settings/config.yaml
kind: screen
id: settings
title: Settings
description: Profile and workspace settings with sidebar and form sections
status: stable
schemaVersion: "2.0"
tags: [screen, settings, profile]
states:
  default: { description: "Profile section with avatar, name, email fields and workspace preferences" }
  saving: { description: "Success toast visible after saving changes" }
