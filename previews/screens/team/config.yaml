# previews/screens/team/config.yaml
kind: screen
id: team
title: Team
description: Team management with sidebar, member list, and status indicators
status: stable
schemaVersion: "2.0"
tags: [screen, team, members]
states:
  default: { description: "Member list with avatars, roles, and online/offline indicators" }
  empty: { description: "Zero-state with invite-teammates prompt" }
  inviting: { description: "Invite modal overlay with email input and role picker" }
  pending: { description: "Member list with pending-invite rows showing expiry countdown" }
