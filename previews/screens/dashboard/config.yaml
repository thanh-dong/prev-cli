kind: screen
id: dashboard
title: Dashboard
description: App shell with sidebar, project cards grid, activity feed, and task summary
status: stable
schemaVersion: "2.0"
tags: [screen, dashboard, overview]
states:
  default: { description: "Full dashboard with projects, activity feed, and task counts" }
  empty: { description: "Zero-state with create-project prompt and empty activity feed" }
