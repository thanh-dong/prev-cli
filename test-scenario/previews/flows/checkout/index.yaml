name: Checkout Flow
description: User journey from cart to payment confirmation
steps:
  - screen: login
    state: default
    note: User must authenticate before checkout
    trigger: Click "Checkout" button
  - screen: login
    state: loading
    note: Authenticating user credentials
  - screen: login
    state: error
    note: Display error if authentication fails
    trigger: Invalid credentials
